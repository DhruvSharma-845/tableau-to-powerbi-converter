expression Expressions

    expression 'Orders (Sample-Superstore-Subset-Excel_0)' =
        ```
        let
    Source = #table(
        type table [Column1 = text],
        {}
    )
in
    Source
        ```